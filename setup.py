from path import Path
from setuptools import find_packages, setup

setup(
    name="reading-group",
    version="0.0.1",
    author="Your Name",
    description="A simple path manipulation library",
    packages=find_packages(),
)
